# OpenAI Configuration (Optional - required for AI features)
OPENAI_API_KEY=your_openai_api_key_here

# CORS Configuration (Optional)
# Comma-separated list of allowed origins for CORS
# Use "*" for development (allows all origins)
# In production, specify exact origins: https://app.example.com,https://www.example.com
# Default: "*" (all origins)
CORS_ORIGINS=*

# Database Configuration (REQUIRED)
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database_name
# This is used for all data persistence (tasks, plugins, MCP servers, etc.)
# Default: postgresql://postgres:postgres@localhost:5432/superdashboard
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/superdashboard

# Jira Configuration (Optional - required for Jira plugin)
JIRA_URL=https://your-domain.atlassian.net
JIRA_EMAIL=your-email@example.com
JIRA_API_TOKEN=your-api-token
JIRA_JQL=project = "YOUR_PROJECT_KEY" order by created DESC

# Twilio Configuration (Optional - required for WhatsApp plugin)
TWILIO_ACCOUNT_SID=your_account_sid
TWILIO_AUTH_TOKEN=your_auth_token
TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886

# Moltbook Configuration (Optional - required for Moltbook plugin)
# Moltbook is a social network for AI agents
# Get your API key by registering at https://moltbook.com
MOLTBOOK_API_KEY=your_moltbook_api_key_here
MOLTBOOK_BASE_URL=https://www.moltbook.com/api/v1
